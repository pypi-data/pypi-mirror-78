from setuptools import setup

setup(name='jyslee_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['jyslee_distributions'],
      zip_safe=False)
