#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['sql_convertor',
 'sql_convertor.dest',
 'sql_convertor.source',
 'sql_convertor.source.sql',
 'sql_convertor.tests']

package_data = \
{'': ['*'],
 'sql_convertor': ['.pytest_cache/*',
                   '.pytest_cache/v/*',
                   '.pytest_cache/v/cache/*']}

install_requires = \
['pyparsing']

entry_points = \
{'console_scripts': ['convertor = sql_convertor:main']}

setup(name='sql-convertor',
      version='0.0.7',
      description='Convert SQL table definition to peewee/django/... model definition',
      author='zpoint',
      author_email='zp0int@qq.com',
      url='https://github.com/zpoint/sql-convertor',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.5.2',
     )
