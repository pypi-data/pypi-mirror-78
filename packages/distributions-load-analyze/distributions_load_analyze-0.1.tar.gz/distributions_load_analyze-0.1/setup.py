from setuptools import setup

setup(name='distributions_load_analyze',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_load_analyze'],
      zip_safe=False)
