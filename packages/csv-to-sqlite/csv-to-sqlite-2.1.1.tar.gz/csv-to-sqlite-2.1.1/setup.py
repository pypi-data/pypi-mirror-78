#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['click']

entry_points = \
{'console_scripts': ['csv-to-sqlite = csv_to_sqlite:start']}

setup(name='csv-to-sqlite',
      version='2.1.1',
      description='A script that processes the input CSV files and copies them into a SQLite database.',
      author='Ladislav Benc',
      author_email='ladislav.benc@protonmail.com',
      url='https://github.com/zblesk/csv-to-sqlite',
      py_modules=['csv_to_sqlite'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3',
     )
