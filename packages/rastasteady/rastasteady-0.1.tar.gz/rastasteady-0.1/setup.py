from setuptools import setup, find_packages

setup(
    name='rastasteady',
    version='0.1',
    description='',
    long_description='',
    install_requires=[
        'pyfiglet',
    ],
    packages=find_packages(),
    zip_safe=False,
)
