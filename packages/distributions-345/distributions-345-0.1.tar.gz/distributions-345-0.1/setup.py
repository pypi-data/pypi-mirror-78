from setuptools import setup

setup(name='distributions-345',
      version='0.1',
      description='Binomial and Gaussian distributions',
      packages=['distributions-345'],
      zip_safe=False,
     author = 'munirat.sulaimon')
