from setuptools import setup

setup(name='robin_pl',
      version='0.0',
      description='Very bad programming language in russian!',
      packages=['robin_pl'],
      author_email='foton-pc@inbox.ru',
      zip_safe=False)
