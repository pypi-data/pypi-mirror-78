#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['rename_musicfiles']

package_data = \
{'': ['*']}

package_dir = \
{'': 'src'}

install_requires = \
['phrydy >=2.0.0']

entry_points = \
{'console_scripts': ['rename_musicfiles = rename_musicfiles:main']}

setup(name='rename_musicfiles',
      version='0.1.2',
      description='A very simple script that renames audio files based on their tags to a hardcoded pattern',
      author='Marc Richter',
      author_email='mail@marc-richter.info',
      url='https://github.com/The-Judge/rename_musicfiles',
      packages=packages,
      package_data=package_data,
      package_dir=package_dir,
      install_requires=install_requires,
      entry_points=entry_points,
     )
